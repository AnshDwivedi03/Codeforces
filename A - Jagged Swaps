Q. -> https://codeforces.com/problemset/problem/1896/A
Ans->
Check for first character only if it is 1 or not..
#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int cases;
    cin>>cases;
    while(cases--){
        int n;
       cin>>n;
       vector<int>v(n);
       for(int i=0;i<n;i++){
           cin>>v[i];
       }
       if(v[0]==1){
           cout<<"Yes"<<endl;
       }
       else{
            cout<<"No"<<endl;
       }
       
    }
 
    return 0;
}
